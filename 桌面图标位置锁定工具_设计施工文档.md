# 桌面图标位置锁定工具 — 设计施工文档

> 面向 AI Coding Agent 的实现规格说明。目标平台：Windows 10/11。
> 核心诉求：锁定图标位置 → 分辨率变化时自适应（等比映射）→ 允许对 1K/2K/4K 等具体分辨率单独锁定精确坐标 → 零遮罩、零视觉差异、极低性能开销。

---

## 1. 项目目标与验收标准

| 编号 | 需求 | 验收标准 |
|---|---|---|
| R1 | 记住每个桌面图标的位置 | 重启资源管理器/重启系统后图标位置不变 |
| R2 | 分辨率变化时自适应 | 无对应分辨率精确配置时，按等比例换算坐标，图标不重叠、不越界 |
| R3 | 分辨率级精确锁定 | 用户可为某一分辨率（如 1920x1080 / 2560x1440 / 3840x2160）单独保存一套坐标，切换到该分辨率时优先使用该坐标而非等比换算 |
| R4 | 无遮罩/无叠加层 | 程序不得创建任何可见窗口、透明层、图标绘制层；操作的是**真实桌面 ListView 中的图标**，不是模拟渲染 |
| R5 | 性能开销极低 | 常驻内存 < 20MB，CPU 空闲时 ~0%，不使用高频轮询（默认应事件驱动） |
| R6 | 抗干扰 | 用户手动拖动图标 / explorer.exe 崩溃重启 / 插拔显示器 / 修改 DPI 缩放，均不破坏已保存的布局逻辑 |
| R7 | 多显示器支持 | 每个显示器的图标各自按其分辨率/位置独立处理 |

---

## 2. 技术原理总览

Windows 桌面图标本质上是 `Progman` 窗口下的一个标准 `SysListView32` 控件（在启用毛玻璃/壁纸时，路径会经过一个 `WorkerW` 窗口，但最终容器仍是同一个 ListView）。这意味着：

- **图标位置是可编程读写的真实数据**，无需截图、无需绘制、无需叠加窗口。
- Windows 提供两条读写路径：
  1. **Win32 消息**：`LVM_GETITEMPOSITION` / `LVM_SETITEMPOSITION` 直接对 ListView 句柄发送消息，按**索引**存取坐标。
  2. **COM 接口 `IFolderView2`**（推荐，身份更稳）：通过 `IShellWindows → IShellBrowser → IFolderView2`，可以按 **PIDL（图标的持久化身份）** 读写 `GetItemPosition` / `SetItemPosition`，不受图标顺序变化影响。

因为最终写回的就是系统真实的图标坐标，所以天然满足 R4（无遮罩、原生观感）——我们不是在图标上面"画"了一层假象，而是直接搬动了图标本身。

---

## 3. 总体架构

```
┌─────────────────────────────────────────────────────────┐
│                      Tray 常驻服务进程                     │
│  (C# .NET / C++ Win32，均可，推荐 C# 便于开发效率与稳定性)    │
├─────────────────────────────────────────────────────────┤
│ [1] 事件监听层                                             │
│   - WM_DISPLAYCHANGE (分辨率/DPI变化广播，需隐藏消息窗口接收)  │
│   - WM_DEVICECHANGE  (显示器插拔)                           │
│   - SetWinEventHook(EVENT_OBJECT_LOCATIONCHANGE)           │
│        只挂钩桌面 ListView 句柄，监听图标被移动（含系统自动重排）│
│   - 进程监视：轮询极低频（如 5s）检测 explorer.exe 是否重启    │
│              （仅此一处允许"轮询"，且开销可忽略）              │
├─────────────────────────────────────────────────────────┤
│ [2] 图标身份与坐标读写层 (IconAccessor)                     │
│   - 获取桌面 IFolderView2                                  │
│   - EnumItems 遍历，取每个图标的 PIDL → 转为稳定 Key         │
│     （建议用"完整路径"或"PIDL 的规范名称"作为 Key，          │
│       避免用显示名称，因为可能重名/本地化）                   │
│   - GetItemPosition(pidl) / SetItemPosition(pidl, pt)       │
├─────────────────────────────────────────────────────────┤
│ [3] 布局存储层 (LayoutStore)                                │
│   - 配置文件：JSON，按"显示器指纹 + 分辨率 + DPI"分 Profile   │
│   - 每个 Profile 下：{ IconKey: {x, y} }                    │
│   - 额外保存一个"基准 Profile"（用户初次锁定时的分辨率）      │
│     用于其它未手动配置的分辨率做等比换算的源数据              │
├─────────────────────────────────────────────────────────┤
│ [4] 自适应换算引擎 (AdaptiveMapper)                          │
│   - 输入：目标分辨率 + 基准 Profile 坐标                     │
│   - 输出：等比例换算后的坐标（含图标网格对齐、边界裁剪）        │
├─────────────────────────────────────────────────────────┤
│ [5] 决策调度器 (LockController)                             │
│   分辨率变化事件触发 →                                       │
│     若存在该分辨率的"精确 Profile" → 直接应用（R3）           │
│     否则 → 调用 AdaptiveMapper 按基准 Profile 换算（R2）      │
│   图标位置被意外移动（用户拖动之外的系统自动重排）→ 还原        │
├─────────────────────────────────────────────────────────┤
│ [6] 托盘 UI（极简）                                          │
│   - 图标：锁定/解锁状态切换                                   │
│   - 右键菜单：                                               │
│     "保存当前布局为本分辨率的精确配置"                        │
│     "保存当前布局为基准配置（用于自适应换算）"                 │
│     "解锁位置管理…"（列表+编辑，可选高级功能）                │
└─────────────────────────────────────────────────────────┘
```

---

## 4. 关键模块详细设计

### 4.1 图标身份与坐标读写层（IconAccessor）

**接口获取步骤**：

1. `CoCreateInstance(CLSID_ShellWindows)` 得到 `IShellWindows`。
2. 遍历窗口找到桌面（`IShellWindows::FindWindowSW`，参数用 `CSIDL_DESKTOP`，`SWC_DESKTOP`），得到 `IDispatch`，QI 到 `IServiceProvider`。
3. `QueryService(SID_STopLevelBrowser, IID_IShellBrowser)` 得到 `IShellBrowser`。
4. `IShellBrowser::QueryActiveShellView` 得到 `IShellView`，QI 到 `IFolderView2`。

**读写坐标**：

```csharp
// 伪代码 / C# 互操作示意
IFolderView2 folderView = GetDesktopFolderView2();
int count = folderView.ItemCount(SVGIO.SVGIO_ALLVIEW);

for (int i = 0; i < count; i++)
{
    IntPtr pidl = folderView.Item(i); // 获取该图标的 PIDL
    string key = GetStableKeyFromPidl(pidl); // 用完整路径/规范名生成稳定 Key
    POINT pt;
    folderView.GetItemPosition(pidl, out pt); // 读取当前坐标

    // ... 写入
    folderView.SetItemPosition(pidl, ref targetPt);
}
```

> **稳定 Key 生成建议**：优先用 `SHGetNameFromIDList(pidl, SIGDN_DESKTOPABSOLUTEPARSING)` 得到完整路径作为 Key；对于"这台电脑""回收站"等虚拟项，用其固定 CLSID 字符串代替。

**写入后需暂时关闭自动排列**（否则 SetItemPosition 可能被系统重新对齐覆盖）：
- 检测桌面右键菜单"自动排列图标 / 将图标与网格对齐"两个开关状态（通过 `LVM_GETEXTENDEDLISTVIEWSTYLE` 判断 `LVS_EX_AUTOARRANGE` 等，或直接读注册表 `HKCU\Software\Microsoft\Windows\Shell\Bags\1\Desktop\FFlags`）。
- 若用户开启了"自动排列"，需要在写坐标前临时关闭该标志（`LVM_SETEXTENDEDLISTVIEWSTYLE`），写入完成后可选择性恢复，避免破坏用户其他偏好。也可以在托盘菜单里提示用户"建议关闭自动排列，否则无法精确锁定"。

---

### 4.2 布局存储层（LayoutStore）

**配置文件结构（JSON）**：

```json
{
  "version": 1,
  "baseProfile": {
    "resolution": "1920x1080",
    "dpi": 100,
    "monitorFingerprint": "DELL_U2720Q_...",
    "icons": {
      "C:\\Users\\xxx\\Desktop\\项目文档.lnk": { "x": 40,  "y": 60 },
      "::{20D04FE0-3AEA-1069-A2D8-08002B30309D}": { "x": 40, "y": 160 }
    }
  },
  "exactProfiles": [
    {
      "resolution": "2560x1440",
      "dpi": 100,
      "monitorFingerprint": "DELL_U2720Q_...",
      "icons": {
        "C:\\Users\\xxx\\Desktop\\项目文档.lnk": { "x": 52,  "y": 78 }
      }
    },
    {
      "resolution": "3840x2160",
      "dpi": 150,
      "monitorFingerprint": "LG_27UL850_...",
      "icons": { "...": { "x": 0, "y": 0 } }
    }
  ]
}
```

- `monitorFingerprint`：由 `EDID` 制造商/型号信息或 `\\.\DISPLAY1` 设备实例路径 hash 生成，用于区分"同一台电脑换了显示器"的情况，避免误用错误分辨率的配置。
- `exactProfiles` 用 `resolution + dpi + monitorFingerprint` 三元组做唯一匹配键。
- 多显示器场景：`baseProfile`/`exactProfiles` 应按显示器分别存储（可扩展为数组，每个显示器一份，通过显示器指纹关联）。

**存储位置**：`%LOCALAPPDATA%\DesktopIconLock\layout.json`，每次成功保存都写入带时间戳的备份（如 `layout.bak.20260831.json`，仅保留最近 5 份），防止配置损坏导致图标"复位丢失"。

---

### 4.3 自适应换算引擎（AdaptiveMapper）

当目标分辨率没有精确 Profile 时，使用基准 Profile 做等比例映射：

```
scaleX = targetWidth  / baseWidth
scaleY = targetHeight / baseHeight

newX = round(baseX * scaleX)
newY = round(baseY * scaleY)
```

**需要处理的边界情况**：

1. **DPI 差异**：图标网格间距（`LVM_GETITEMSPACING`）会随 DPI 变化，纯像素等比缩放可能导致图标间距与系统网格不一致。建议换算后再执行一次**吸附对齐**：将换算坐标 snap 到目标分辨率下系统实际的图标网格步进（水平/垂直间距），取最近网格点。
2. **越界保护**：换算结果做 `clamp`，确保 `x ∈ [0, workAreaWidth - iconWidth]`，`y` 同理，防止图标跑出屏幕可视区域（尤其是任务栏占用区域要排除，使用 `SystemParametersInfo(SPI_GETWORKAREA)` 取工作区而非整个屏幕）。
3. **图标数量不一致**（比如新分辨率下用户又新建了桌面文件）：没有历史坐标的图标不做处理，保持系统默认位置，仅对"已知 Key"的图标生效。
4. **纵横比差异较大**（如从 16:9 换到 21:9 显示器）：可提供开关——"仅锁定 X/Y 中较小偏移量的一侧" 或 "保持相对屏幕中心距离" 作为可选换算策略，默认用简单等比例即可满足大部分场景。

---

### 4.4 事件监听与调度（避免轮询、保证低开销）

| 触发源 | 监听方式 | 说明 |
|---|---|---|
| 分辨率/DPI 变更 | 隐藏消息窗口 `WndProc` 接收 `WM_DISPLAYCHANGE` | 系统主动广播给顶层窗口，零轮询 |
| 显示器插拔 | `RegisterDeviceNotification` + `WM_DEVICECHANGE` | 同上 |
| 图标被移动（含系统自动重排、用户拖动） | `SetWinEventHook(EVENT_OBJECT_LOCATIONCHANGE, ..., hwndDesktopListView, ...)` | 仅挂钩桌面 ListView 这一个句柄，事件量极小，几乎无开销 |
| explorer.exe 重启 | 低频轮询（如每 5 秒 `Process.GetProcessesByName("explorer")` 判断 PID 是否变化），或更优地用 `WMI __InstanceCreationEvent` 订阅进程创建事件（完全事件驱动，进一步降低开销） | explorer 重启后桌面 ListView 句柄失效，需重新获取 IFolderView2 并重新应用坐标 |

**防抖设计**：分辨率变化 / 图标位置变化事件到达后不要立即处理，启动一个 300~500ms 的防抖定时器，多次事件合并只处理最后一次，避免拖动图标或显示器切换过程中的中间态触发多次无意义写入。

**"用户主动拖动"与"系统被动移动"的区分**（可选高级功能，用于决定是否要"纠正"回锁定位置）：
- 简单策略：只要"锁定模式"开启，任何位置变化都会在防抖后被纠正回配置值（即真正意义上的"锁定"）。
- 更友好策略：锁定模式下完全阻止移动（每次检测到变化立即纠正）；若用户想调整，需先在托盘菜单点击"临时解锁"或直接"保存当前为新配置"来更新基准值。

---

### 4.5 决策调度器（LockController）核心流程

```
on DisplayChangedEvent(newResolution, newDpi, monitorFingerprint):
    debounce(300ms)
    profile = LayoutStore.FindExactProfile(newResolution, newDpi, monitorFingerprint)
    if profile != null:
        IconAccessor.ApplyPositions(profile.icons)      // R3: 精确锁定
    else:
        mapped = AdaptiveMapper.Map(LayoutStore.baseProfile, newResolution, newDpi)
        IconAccessor.ApplyPositions(mapped)              // R2: 自适应换算

on IconLocationChangedEvent(icon, newPos):
    if lockEnabled:
        debounce(300ms)
        expectedPos = CurrentAppliedProfile.icons[icon.key]
        if newPos != expectedPos:
            IconAccessor.SetItemPosition(icon, expectedPos)  // 纠正回锁定位置

on ExplorerRestartedEvent():
    wait(1000ms)  // 等待新 explorer 完成桌面初始化
    ReacquireFolderView2()
    ApplyCurrentProfileAgain()
```

---

## 5. 技术栈建议

| 组件 | 推荐 | 备注 |
|---|---|---|
| 主程序语言 | C#（.NET 8, WinForms/WPF 仅用于托盘图标） | 开发效率高，P/Invoke 调用 Win32/COM 成熟稳定；也可用 C++/Win32 追求极致体积和性能，若 Agent 更擅长 C++ 也可选用 |
| COM 互操作 | `System.Runtime.InteropServices` + 手写 `IFolderView2` COM 接口定义（.NET 无内置封装，需要自定义 `[ComImport][Guid(...)]` 接口声明） | 网上有大量开源参考实现（如 DesktopOK、IconRestorer 类工具的思路） |
| 事件钩子 | `user32.dll` 的 `SetWinEventHook` / `RegisterDeviceNotification` | 通过 P/Invoke |
| 配置存储 | `System.Text.Json` | 轻量、无额外依赖 |
| 开机自启 | 写入 `HKCU\...\Run` 或注册计划任务（无 UAC 弹窗，推荐计划任务方式） | |
| 打包 | 单文件自包含发布（`dotnet publish -r win-x64 --self-contained`） | 减少用户环境依赖 |

---

## 6. 模块/文件划分建议（供 Agent 直接落地为项目结构）

```
DesktopIconLock/
├── Program.cs                  // 入口，托盘初始化，隐藏消息窗口
├── Native/
│   ├── ShellInterop.cs         // IFolderView2, IShellWindows 等 COM 接口 P/Invoke 声明
│   ├── User32.cs               // SetWinEventHook, WM_DISPLAYCHANGE 相关声明
│   └── DisplayInfo.cs          // 获取分辨率/DPI/显示器指纹（EDID 或设备路径）
├── Core/
│   ├── IconAccessor.cs         // 4.1 节
│   ├── LayoutStore.cs          // 4.2 节
│   ├── AdaptiveMapper.cs       // 4.3 节
│   └── LockController.cs       // 4.4 / 4.5 节
├── Tray/
│   └── TrayContext.cs          // 托盘菜单与用户交互
└── Config/
    └── layout.schema.json      // 配置文件结构定义（便于校验）
```

---

## 7. 测试用例设计

| 场景 | 步骤 | 期望结果 |
|---|---|---|
| 基础锁定 | 保存布局 → 拖动某图标 → 等待防抖 | 图标自动弹回原位置 |
| 分辨率切换（无精确配置） | 1080p 下保存基准 → 切到 1440p | 图标按等比例换算移动，无重叠/越界 |
| 分辨率切换（有精确配置） | 分别在 1080p / 1440p / 4K 保存精确配置 → 依次切换分辨率 | 每次都精确复现该分辨率下保存的坐标，而非等比计算值 |
| DPI 单独变化（分辨率不变） | 修改系统缩放 100%→150% | 图标网格对齐正确，无越界 |
| 显示器插拔 | 插入/拔出外接显示器 | 对应显示器图标按其自身分辨率的配置/换算处理，主屏不受影响 |
| 被remote远程连接 | 客户端更改rdp连接分辨率 | 对应显示器图标按其自身分辨率的配置/换算处理，主屏不受影响 |
| explorer.exe 崩溃重启 | 手动结束 explorer.exe 并等待自动重启 | 桌面恢复后自动重新应用当前锁定的布局 |
| 新增图标 | 桌面新建一个文件 | 不影响已有图标位置，新图标可被系统自动放置，之后可手动纳入锁定 |
| 长时间空闲性能 | 挂机 1 小时不做任何操作 | CPU 占用趋近 0%，内存无持续增长（无内存泄漏） |
| 视觉一致性 | 全程截图对比开启/关闭本工具前后的桌面 | 完全一致，无任何多余窗口/边框/高亮框出现在任务管理器或屏幕上 |

---

## 8. 风险与边界情况说明

1. **某些第三方桌面美化工具**（如 Rainmeter 接管桌面、Stardock Fences 等）会替换或叠加自己的图标容器，此时找不到标准 `SysListView32`，需要在启动时做兼容性检测并给出提示，而非静默失败。
2. **虚拟机/远程桌面分辨率频繁变化**场景下，防抖时间可能需要调大，避免频繁写入。
3. **管理员权限**：一般无需管理员权限即可操作当前用户的桌面 ListView；若目标是"所有用户桌面"（Public Desktop）项，可能涉及权限问题，建议仅处理当前用户桌面。
4. **图标 Key 失效**：若用户重命名或移动了桌面文件，原 Key 会失效，导致该图标"脱离锁定"，工具应能优雅忽略而不报错，并在 UI 里标记"未匹配项"供用户重新关联（可选功能）。

---

## 9. 交付物清单

- [ ] 可执行程序（含托盘图标）
- [ ] 首次运行引导（提示关闭"自动排列图标"选项，说明其必要性）
- [ ] 配置文件读写与备份机制
- [ ] 完整覆盖第 7 节测试用例的手动/自动化测试记录
- [ ] README：说明锁定原理、多分辨率配置方法、常见问题（如遇美化工具冲突时的处理）

---


## 10. 开源、代码可直接参考的

npiegdon/desktop-saver — Windows 托盘常驻程序,持续跟踪并可恢复桌面图标位置。是常驻托盘 + 持续跟踪这个思路(对应文档里的事件监听层)最贴近的小型开源实现,代码量不大,适合直接抄读 IFolderView2/ListView 调用部分。 
GitHub
Stanislav-Povolotsky/DesktopIconsSaveRestoreTool — 命令行工具,用于保存和恢复桌面图标位置。命令行、无 GUI 干扰,核心的坐标读写逻辑会更纯粹、更容易抽取成模块。 
GitHub
s0d3s/Positioner — 带 GUI 的桌面图标保存/恢复管理工具。如果想参考"托盘 UI + 多套布局管理"的交互设计可以看这个。 
GitHub
mapi68/desktop-icon-backup-manager — 和咱们需求最像的一个,MIT 开源、Python 写的。支持跨分辨率自适应缩放、多显示器感知、恢复前的差异预览和统计面板,并且明确提到分辨率/DPI变化后图标会自动排列、切换显示器后布局被打乱、系统重启后位置复位这些痛点,和文档里第 8 节"风险与边界情况"几乎一一对应,非常适合作为"别人是怎么处理这些坑"的参考。不过它是 Python 实现,如果最终想要"零遮罩、常驻内存 <20MB"的 C#/C++ 版本,只能参考它的思路和坑,代码本身抄不了。 
GitHub
GitHub

**给实现 Agent 的建议顺序**：先实现 4.1（IconAccessor 读写）和 4.2（存储）→ 跑通"保存并原样恢复"的最小闭环 → 再接入 4.4 事件监听 → 最后实现 4.3 自适应换算与 4.5 调度整合，逐步验证第 7 节测试用例。
