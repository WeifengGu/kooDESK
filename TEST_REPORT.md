# DesktopIconLock 自适应布局与开机启动验收报告

## 1. 基线环境

- 当前物理显示模式：`1920x1080 @ 60Hz`
- 原始缩放：`125%`（DPI 120）
- 原始工作区：`1920x1020`
- 真实桌面图标：`56` 个
- 从当前布局自动分析出的网格：
  - 原点：`(17, 2)`
  - 水平步进：`95`
  - 垂直步进：`122`

基线截图：

`artifacts/adaptive-tests/baseline-1920x1080-125pct-physical.png`

最终恢复截图：

`artifacts/adaptive-tests/final-restored-1920x1080-125pct-desktop.png`

最终恢复后将 56 个图标逐项与基线坐标比较，结果：

```text
coordinateDifferences=0
```

## 2. 实际缩放比例测试

以下测试均通过 Windows“设置 → 系统 → 屏幕 → 缩放”真实切换，并等待两阶段防抖/稳定校验完成后截图。

| 物理分辨率 | 缩放 | 图标数 | 重复坐标 | 越界 | 结果 | 截图 |
|---|---:|---:|---:|---:|---|---|
| 1920x1080 | 100% | 56 | 0 | 0 | 通过 | `test-1920x1080-100pct-desktop-v3.png` |
| 1920x1080 | 125% | 56 | 0 | 0 | 通过，且精确恢复基线 | `final-restored-1920x1080-125pct-desktop.png` |
| 1920x1080 | 150% | 56 | 0 | 0 | 通过 | `test-1920x1080-150pct-desktop-v3.png` |
| 1920x1080 | 175% | 56 | 0 | 0 | 通过 | `test-1920x1080-175pct-desktop-v3.png` |

测试中确认：

- 左侧文件夹组、中央应用组、右侧应用矩阵仍保持区域结构和行列顺序。
- 高缩放导致原网格容量减少时，会在同一目标行内优先寻找空位；同一行确实放不下时才换行。
- 不再出现底部图标被错误挤到顶部、早期已放置图标被后续写入顶走的问题。
- 每次写入后都会跨进程回读坐标验证。

## 3. 分辨率测试说明

当前 Windows 会话的显示器驱动只公开一个物理模式：

```text
1920x1080@60
```

同时 Windows 设置里的“显示器分辨率”下拉框处于禁用状态。因此无法在该会话中真正切换到 1600x900 或 1280x720；直接调用 `ChangeDisplaySettings` 也返回目标模式不存在。

为继续验证分辨率自适应，而不是跳过测试，本次采用以下等价验证方式：

1. 停止常驻纠正器。
2. 使用真实基准 Profile 计算目标分辨率的工作区网格。
3. 将计算后的 56 个目标坐标真实写入桌面 `SysListView32`。
4. 按目标分辨率裁切物理桌面截图。
5. 检查重复坐标、越界和视觉结构。
6. 启动常驻程序恢复当前真实模式。

| 目标工作区 | DPI | 图标数 | 重复坐标 | 最大坐标 | 结果 | 截图 |
|---|---:|---:|---:|---|---|---|
| 1600x900 | 96 / 100% | 56 | 0 | `(1458,702)` | 通过 | `test-simulated-1600x900-100pct-desktop.png` |
| 1280x720 | 96 / 100% | 56 | 0 | `(1154,502)` | 通过 | `test-simulated-1280x720-100pct-desktop.png` |

两组测试使用的都是真实桌面图标，不是模拟渲染层；仅显示模式本身由目标工作区代替，因为当前驱动没有其它可切换模式。

## 4. 自动化测试

`TestRunner.exe` 最终结果：

```text
DesktopIconLock v2 自适应核心测试
[PASS] 真实物理显示信息
[PASS] 当前布局网格分析
[PASS] 同配置映射恒等性
[PASS] 跨分辨率与DPI无重叠映射
[PASS] 精确Profile严格区分DPI
[PASS] v2配置独立目录持久化
RESULT 6/6
```

完整输出：

`artifacts/adaptive-tests/automated-test-output.txt`

## 5. 开机启动功能测试

新增托盘菜单：

```text
🚀 开机启动
```

实现方式：

- 注册表：`HKCU\Software\Microsoft\Windows\CurrentVersion\Run`
- 值名：`DesktopIconLock`
- 值内容：带双引号的当前 `DesktopIconLock.exe` 绝对路径
- 无需管理员权限
- 托盘菜单会根据注册表实际状态显示勾选

真实测试流程：

1. 启用开机启动。
2. 使用 `reg query` 验证值存在且路径正确。
3. 关闭开机启动。
4. 验证注册表值已删除。
5. 最终恢复为测试前的“未启用”状态。

## 6. 截图总览

`artifacts/adaptive-tests/adaptive-test-contact-sheet.png`

该总览图包含：

- 原始/恢复 1920x1080 125%
- 实际 1920x1080 100%
- 实际 1920x1080 150%
- 实际 1920x1080 175%
- 目标工作区 1600x900 100%
- 目标工作区 1280x720 100%

## 7. 历史布局管理验收

新增功能：

- 每次用户点击“保存当前布局”后：
  - 保存完整图标坐标Profile。
  - 临时最小化当前可见窗口。
  - 捕获完整虚拟桌面PNG。
  - 截图完成后恢复截图前可见窗口。
- 历史菜单行格式：

```text
xxxx年xx月xx日xx时xx分xx秒 分辨率xxxx x xxxx 缩放 xxx% 序号x
```

- 同一物理分辨率和DPI最多保留5条。
- 不同分辨率和DPI组合分别保存，组合数量不限。
- 自动轮转跳过已设为基准的记录。
- 悬停记录显示截图和布局信息。
- 点击记录后显示“删除、取消、基准、应用”按钮。
- 删除同时清理Profile和PNG；删除基准记录时同步清除基准标记。
- 应用历史记录时：
  - 当前显示模式一致：直接应用精确坐标。
  - 当前显示模式不同：执行结构保持型自适应换算。

便携数据目录（位于程序同级目录，绿色免安装）：

```text
dist\layout.json
dist\logs\
dist\history\records\
dist\history\screenshots\
```

悬浮预览和操作按钮截图：

`artifacts/adaptive-tests/history-preview-actions.png`

自动化测试新增结果：

```text
[PASS] 历史布局同模式最多5条且基准不被轮转删除
[PASS] 历史布局截图、基准和删除
RESULT 8/8
```

## 8. 菜单微调验收

- 一级菜单和历史菜单中不再使用Emoji。
- 删除“立即恢复/重置图标位置”。
- 删除一级“保存为基准配置”。
- 基准只能在历史记录预览按钮中设置。
- “打开白话审计日志目录”已改为“日志目录”。
- 保留“开机启动”菜单，但去除Emoji。
