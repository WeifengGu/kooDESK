using System;using System.Drawing;using System.Windows.Forms;using DesktopIconLock.Core;using DesktopIconLock.Native;using DesktopIconLock.Tray;class H{[STAThread]static void Main(){DisplayInfo.EnablePerMonitorDpiAwareness();Application.EnableVisualStyles();var store=new LayoutStore();var history=new HistoryStore(store.ConfigDirectory);var records=history.GetRecords();if(records.Count==0)return;var form=new HistoryPreviewForm();form.PinActionPanel(records[0],new Point(600,400));var timer=new Timer();timer.Interval=4500;timer.Tick+=delegate{timer.Stop();form.Close();};timer.Start();Application.Run(form);}}
